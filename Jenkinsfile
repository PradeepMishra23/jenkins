pipeline {
    agent any
    stages {
        stage('stage1-terraform init') {
            steps {
                echo 'terraform init'
            }
        }
		stage('stage2-terraform plan') {
            steps {
                echo 'terraform plan'
            }
        }
    }
}
